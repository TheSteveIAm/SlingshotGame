using UnityEngine;

public class PlayerFire : MonoBehaviour
{
    public float fireStrength = 500;
    public Color nextColor = Color.red;

    //void Update()
    //{
    //    Vector3 rot = transform.parent.forward;
    //    rot.y = 0;

    //    transform.rotation = Quaternion.Euler(rot);
    //}
}